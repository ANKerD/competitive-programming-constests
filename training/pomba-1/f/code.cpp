#include <bits/stdc++.h>
using namespace std;
#define fi first
#define se second
#define ii pair<int, int>
#define vi vector<int>
#define vll vector<ll>
#define vii vector<ii>
#define sc scanf
#define maxn -1982979

struct hotel {
  int l, r, id, c;
  hotel(): l(0), r(0), id(0), c(0) {}
  hotel(int _l, int _r, int _id, int _c) {
    l = _l;
    r = _r;
    id = _id;
    c = _c;
  }
} idx[MAX];
int dp[370][57][57];

vector<int> graph[MAX];
int main () {
  int y, n, s; scanf("%d %d %d", &y, &n, &s);
  
  for(int i = 1; i <= y)
  return 0;
}